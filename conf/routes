# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /ip                         controllers.IPController.getIP()

GET     /facts                      controllers.FactsController.getFact()
GET     /facts/:n                   controllers.FactsController.getFacts(n: Integer)
